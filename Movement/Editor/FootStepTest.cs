using UnityEngine;
using UnityEditor;
using NUnit.Framework;

public class FootStepTest {

	[Test]
	public void EditorTest() {
        /*FootSteps footStep = new FootSteps();
        HeadBobbing headBob = new HeadBobbing();
        headBob.curLocalPosition.y = 1.9f;

        footStep.PlayFootStep(2);
		Assert.AreEqual(1, footStep.clipNum);*/
	}
    
}
